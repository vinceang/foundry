---
import GuitarPage from "../../components/GuitarPage.astro";

export function getStaticPaths() {
  return ["clasica", "blanca", "negra"].map((slug) => ({ params: { slug } }));
}
const { slug } = Astro.params;
---

<GuitarPage locale="en" slug={slug} />
